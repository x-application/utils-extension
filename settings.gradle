pluginManagement {
    repositories {
        maven {
            url 'https://maven.aliyun.com/repository/gradle-plugin'
        }
    }
}
rootProject.name = 'utils-extension'

include 'common-base'

project(':common-base').projectDir = new File(settingsDir, './modules/common/base')